# Minimal, pip-installable requirements for `baselines/sac_baseline`.
#
# Note:
# - The original upstream repo pinned very old versions (e.g. torch==1.5.1) and included conda-local paths.
# - This file keeps the baseline runnable in a modern environment.

numpy==2.2.6
pandas==2.3.2
gym==0.26.2
torch==2.7.0
tensorboard==2.20.0
