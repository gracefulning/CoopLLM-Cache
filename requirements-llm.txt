# Pinned versions for reproducibility.
# These versions are aligned with the provided pip-freeze style environment list.

# Core
numpy==2.2.6
pandas==2.3.2
tqdm==4.67.1

# Hugging Face / TRL stack
torch==2.7.0
transformers==4.53.1
datasets==3.6.0
accelerate==1.10.1
trl==0.18.2
peft==0.17.1
huggingface-hub==0.34.4
safetensors==0.6.2
sentencepiece==0.2.1

# Helpful pinned transitive deps (stabilize tokenizer & text parsing behavior)
tokenizers==0.21.4
regex==2025.8.29

# Logging
tensorboard==2.20.0
wandb==0.21.3

# Faster HF downloads (optional)
hf_transfer==0.1.9

# Optional speedups (mostly Linux + NVIDIA)
bitsandbytes==0.47.0; platform_system == "Linux"
unsloth==2025.7.3; platform_system == "Linux"
vllm==0.9.1; platform_system == "Linux"
